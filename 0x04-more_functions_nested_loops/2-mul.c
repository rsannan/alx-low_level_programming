#include "main.h"
/**
* mul - checks for digit
* @a: first number
* @b: second number
*
* Return: product of int a & b
*/

int mul(int a, int b)
{
int result;

result = (a) * (b);

return (result);
}
